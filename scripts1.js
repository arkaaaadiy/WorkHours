class WorkHours {
    constructor(el, source) {
        this.$el = document.querySelector(el);
        this.source = source


        if (this.$el) {
            this.printTime
        }
    }

    getGroupedDays() {
        const res = []


    }
}